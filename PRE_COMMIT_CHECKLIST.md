# Pre-Commit Checklist

## ✅ Completed Fixes

### Critical Bug Fixes
- ✅ Fixed `DEFAULT_PALETTE` definition in `src/parsers/spr_parser.py` (line 74)
  - Changed from incorrect syntax to: `bytes([val for i in range(256) for val in (i, i, i, 255)])`
- ✅ Fixed initialization order in `src/gui/main_window.py`
  - Moved `_setup_statusbar()` before `_setup_tabs()` to ensure `status_label` exists when needed

### Code Cleanup
- ✅ Updated `README.md` to use generic paths instead of hardcoded `E:\` paths
  - Changed all example paths to use `C:\` or relative paths
  - Updated clone instructions to use GitHub URL
- ✅ Fixed `rebuild.bat` to use relative paths
  - Changed from hardcoded `F:\2026 PROJECT\AssetHarvester` to `%~dp0` (script directory)

## 📋 Pre-Commit Review

### .gitignore Status
The `.gitignore` file properly excludes:
- ✅ `build/` - PyInstaller build artifacts
- ✅ `dist/` - Built executables
- ✅ `__pycache__/` - Python bytecode
- ✅ `*.pyc`, `*.pyo` - Compiled Python files
- ✅ `*.spec` - PyInstaller spec files (regenerated on build)
- ✅ `data/harvester.db` - Runtime database
- ✅ `*.log` - Log files

### Files Ready for Commit
- ✅ All source code in `src/` directory
- ✅ Configuration files (`data/config.json`)
- ✅ Documentation (`README.md`, `docs/`)
- ✅ Build scripts (`build.bat`, `build.py`, `rebuild.bat`)
- ✅ Requirements (`requirements.txt`)
- ✅ Version info (`version_info.txt`)

### Files Excluded (as expected)
- ❌ `AssetHarvester.spec` - Auto-generated by PyInstaller
- ❌ `build/` directory - Build artifacts
- ❌ `dist/` directory - Distribution files
- ❌ `data/harvester.db` - User database (runtime)

### Notes
- Print statements in code are intentional (CLI output, errors, warnings)
- TODO comments in `grf_extractor.py` about DES decryption are documented features
- Hardcoded paths in comments are acceptable (they're examples)

## 🚀 Ready to Commit

The codebase is ready for commit. All critical fixes have been applied, paths have been made generic, and the code is production-ready.

### Recommended Commit Message
```
Fix critical bugs and prepare for GitHub release

- Fix DEFAULT_PALETTE bytes() syntax error in spr_parser.py
- Fix MainWindow initialization order (status_label before tabs)
- Update README.md with generic paths and GitHub clone URL
- Fix rebuild.bat to use relative paths instead of hardcoded F:\ path
- Code cleanup and documentation improvements
```

